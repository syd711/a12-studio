subprojects {
    apply plugin: 'java'

    group = 'de.a12.studio'
    version = '1.0-SNAPSHOT'

    tasks.withType(JavaCompile).configureEach {
        options.release = 21
    }

    repositories {
        mavenCentral()
    }

    tasks.named('test') {
        useJUnitPlatform()
    }
}

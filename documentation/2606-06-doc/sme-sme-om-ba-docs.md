# sme sme om ba docs

Overview Modeling
Table of Contents
The Overview Model . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 2
Creating a New Overview Model . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 2
Model Settings. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 5
Locales . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 6
Labels and Subtitles . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 6
Roles and Annotations . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 7
Overview . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 8
General Settings . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 10
Columns . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 13
Reference Column. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 14
Expression Column. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 18
Common Settings . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 20
Default Sorting . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 23
Features . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 24
Multi-Selection. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 24
Search and Filters . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 27
Filter Configuration . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 28
Paging Behavior . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 42
Row Height and Action Column Width . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 42
Columns Resize . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 43
Number of Entries . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 43
Initial Load . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 44
Styles. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 44
Custom Actions . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 45
Row Action Group . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 45
Row Action . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 45
Context Menu. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 49
Row Activation . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 49
Title for Interactive Rows . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 50
Screen Reader Column . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 50
Subheader . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 51
Subheader Button . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 53
Footer . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 53
Refactoring. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 53
Overview Model Specific Refactoring . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 53
Supported Operations . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 53
1

-- 1 of 54 --

This documentation is intended for a business analyst audience. Some prior
knowledge of the tools is assumed.
The Overview Model
The Overview Model is a user interface model designed to present lists and tables. While forms are
primarily used to collect data from users, business applications often require tables to display
selected aspects of datasets in an organized manner, as illustrated in Overview Table for Listing
Products. This is the primary purpose of the Overview Model.
Figure 1. Overview Table for Listing Products
Creating a New Overview Model
To add a new Overview Model, first select a Workspace within the SME. Click the Open Workspace
button to browse to the directory where all project-related models are saved.
Reference Types. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 53
Example: Deleting a Column. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 54
Example: Disabling a Feature. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 54
2

-- 2 of 54 --

Figure 2. Open a Workspace
Alternatively, you may select a recent workspace.
Next, click Add/Overview Model.
3

-- 3 of 54 --

Figure 3. Add New Overview Model
A dialog will be displayed with the following fields:
• Folder: The location where the new model will be saved.
• Name: The name of the new model, which should match the file name.
• Document Model: The referenced Document Model. Use the drop-down to select one from the
Workspace.
• Locales: The list of supported locales. Use commas to separate locales, for example, "en, de".
• Roles: The roles that can access this model.
Locales and Roles are added automatically after selecting the Document Model. Click the OK
button; Name, Locales, and Roles will be pre-filled in Model Settings, and the Document Model will
be set in General Settings.
4

-- 4 of 54 --

Figure 4. New Overview Dialog
Model Settings
In the Model Settings tab, Name, Locales, and Roles are filled from the Add Dialog.
Model Version is based on the SME version and cannot be changed.
5

-- 5 of 54 --

Figure 5. Overview Header Details
Locales
For a multilingual application, the supported locales can be defined as part of the model settings.
Click the ADD button in the Locales section to add new locales. The default value is "en" for
English. Additional country codes can be added for multilingual support by using the locale code
(for example, "en_GB", "en_US", etc.).
Figure 6. Locales
Labels and Subtitles
6

-- 6 of 54 --

Figure 7. Label and Subtitle in Overview Engine
The label is displayed in the header at the top of the table. It is also used as the table’s aria-label for
accessibility. The subtitle is the small text below the label.
Label and subtitle are multilingual, so for each locale the corresponding value can be specified.
Checking the Hide Label checkbox will only hide the model label at the header. The aria-label of
the table will still be available.
Figure 8. Multilingual Labels and Subtitles
NOTE If there is no label or it is hidden, then Label, Subtitle, and Number of Entries will
not be shown.
Roles and Annotations
The roles that you can assign to your model are taken from a YAML file in the Workspace. If you
have created your Workspace using the Preview App Control, this file is in the "user" folder and is
called "access-rights.yaml". If no valid YAML file is visible to the SME, then the drop-down will be
blank and the assignment of roles is optional.
7

-- 7 of 54 --

An Annotation is a name-value pair that can be added to the model and some model elements. The
application that contains the Overview Model can access those Annotations and use them within a
custom implementation, for example, to show all fields that have an Annotation in boldface.
To apply an Annotation, fill in the inline repeat with name/value pairs.
Figure 9. Adding an Annotation
Overview
The Overview tab contains the following sections:
• General Settings
• Columns
• Default Sorting
• Features
• Styles
8

-- 8 of 54 --

Figure 10. Tab Overview
9

-- 9 of 54 --

General Settings
The Overview Model can be based on a Document Model or a Query Model. By default, the
Overview Model is based on a Document Model and a drop-down Document Model is shown. All
Document, Combined Document and Composed Document Models of the Workspace are listed.
Figure 11. Document Model Reference
It is also possible to model the Overview Model based on a Query Model.
CAUTION Usage of a Query Model and the Query Model itself are an experimental feature.
To use a Query Model, select Use a Query Model as Reference. A drop-down Query Model is shown
that lists all Query Models of the Workspace.
10

-- 10 of 54 --

Figure 12. Query Model Reference
It is possible to create a new Query Model by clicking the ADD button (plus icon) next to the drop-
down. Via this button, a Query Model that fits to the Overview Model will be created.
11

-- 11 of 54 --

Figure 13. Add Reference Query Model
When creating a new Query Model, the Document Model referenced in the Overview Model is
prefilled as Target Document Model.
If the "Create Query based on Overview Model" switch is turned on, the created Query Model will
contain the following information from the Overview Model:
1. Fields included in Result Set for the Target Document Model Element; derived from the Column
definitions of the Overview Model
2. Paging specification
3. Sorting specification; derived from the Default Sorting of the Overview Model
If a sortable column references data via a to-1 Relationship path, the generated Query sorting is
exported as relationshipField (Data Services RelationshipOrder) instead of a direct root field
reference.
The created Query Model is opened directly in the Query Model Editor while the Overview Model
Editor is still open in the background.
It is also possible to edit a selected Query Model by clicking the EDIT button (pen icon) next to the
drop-down.
The Overview Model Editor will suggest only Fields for reference in Columns and Expressions, that
are marked as In Result in the Query Model.
12

-- 12 of 54 --

CAUTION
The Fields in Result Set of the Query Model will be overwritten by the Overview
Engine at runtime according to the Fields used in the Overview Model and those
specified by the developer. The Paging and Sorting specifications of the Query
Model will be overwritten by the Overview Engine with the settings of the
Overview Model and according to the end user interaction.
If the Query Model specifies 'Exclude All Root Documents', then the result is not
digestible for the Overview Engine. Such Query Models cannot be used without
Custom Code.
The selected Query Model will be used at runtime by the Overview Engine and end users will only
see documents, that are results of the query. It is not possible for an end user to remove restrictions
placed by the Query Model. However, it is possible to add more Filters (Constraints) to the query
and to limit the documents listed in the overview further. These additional Filters can be set by the
end user or by a developer.
For more information about the Query Model please refer to the Query Modeling Documentation.
Columns
Click the ADD button in this section to open a detached repeat screen and add a new column.
Figure 14. Columns
13

-- 13 of 54 --

There are two column types: Reference Column and Expression Column. By default, the ADD screen
will display a Reference Column. To add an Expression Column, change Column Type to
expression.
NOTE
Fields whose data is shown in the Overview must be indexed. Using fields with the
Annotation "indexed" = "false" in Element Reference, Dynamic Suffix, or Expression
will lead to an error in the Simple Model Editor.
Reference Column
A Reference Column defines which field from the Document Model will display via Element
Reference. In addition to the Common Settings, it has special features based on its data type:
• Sortable: for non-multi-select types
• Display Mode: for attachment or multi-select groups only
• Suffix and Summary: for number fields only
Element Reference
A drop-down called Element Reference is populated with non-repeatable fields from the
Document Model in a group/field structure as shown below:
Figure 15. Selecting
Because data from fields in repeats are not unique, the Overview Model cannot use fields from
repeatable groups as Element References.
NOTE
For data type multi-select, the child field may be an enumeration or a string (for
external enumeration). Since Overview Engine does not support external
enumeration yet, multi-select with string field is not supported either.
Sortable
There is a checkbox for determining if the column should be sortable. If selected, Preferred
14

-- 14 of 54 --

Sorting is set to Ascending by default and can be changed to Descending from the drop-down.
Case is ignored during sorting (ignoreCase is set to true).
For columns that reference data via to-1 Relationship paths, sorting is translated to Data Services
relationship sorting (relationshipField / RelationshipOrder).
Figure 16. Sortable Checkbox and Preferred Sorting
NOTE
Sortable is not allowed for data type multi-select. For relationship-based sorting,
only to-1 Relationship traversals are valid, and fields from repeatable groups are
not allowed.
Display Mode
This option is for attachment or multi-select only. The attachment can be displayed as:
• Preview: display supported images. For other file types, an icon will be shown instead.
• Icon with file name: display file icon and file name.
• Icon: display file icon with file name shown as title.
• File name: display file name only.
The multi-select can be displayed as:
• Default: display as a bullet list when row height is not set. Otherwise, it will be a comma-
separated list.
• Comma Separated: always display as a comma-separated list.
Figure 17. Display Mode
Suffix
15

-- 15 of 54 --

Figure 18. Suffix
This optional configuration is for number fields only. The Suffix is multilingual and will be
displayed after the number fields in the overview column. If these number fields appear in the
filter, the suffix will be applied for the filter too.
There are two ways to configure a suffix:
• Static suffix: fill suffix value for each locale and make sure Use Dynamic Suffix is unchecked.
In this way, all rows in the Overview Engine will have the same suffix.
Figure 19. Static Suffix in Overview Table and Filter
• Dynamic suffix: once Use Dynamic Suffix is checked, an enumeration field can be selected to
be used as the suffix.
Figure 20. Use Dynamic Suffix
In this way, each row can have a different suffix. In the example below, the user can see the salary
payment together with the referenced currency.
16

-- 16 of 54 --

Figure 21. Dynamic Suffix in Overview Table and Filter
It is possible to pick a dynamic suffix as a filter condition. Only one suffix is allowed to be selected
per filter.
NOTE
• The summary row will not work with the dynamic suffix.
• The referenced enumeration field will not appear as an independent filter
option once configured as a dynamic suffix.
Summary
Figure 22. Summary
Similar to suffix, this configuration is for number fields only. A summary row will appear at the
footer of the overview table. Check Show sum of all entries checkbox to show the sum of the
column on the summary row.
17

-- 17 of 54 --

Figure 23. Sum of All Entries on the Summary Row
NOTE If no column enables this feature, the summary row will not appear.
Expression Column
Figure 24. Expression Column
An Expression Column is a way to merge multiple field values into a single cell or to add formatting
instructions. In the image above, the Expression Column can display Full Name as a combination of
FirstName and LastName. Here is the result of the FullnameExpression in the Overview Engine:
18

-- 18 of 54 --

Figure 25. Expression Column in Overview Engine
NOTE Sortable is not available for the Expression Column.
To configure an Expression Column, define its Name, Expression, and other Common Settings.
Name
The expression name is displayed at the first cell of the column row, with a small icon beside it. The
name is useful to distinguish it from other columns.
Figure 26. Expression Name
Expression
To define an expression, it is required to understand:
• Document Model structure
• Expression Language
In the example Full Name, the FirstName and LastName belong to the group People.
19

-- 19 of 54 --

Figure 27. Document Model Structure
So the GroupOperation is used here to access them.
Figure 28. Expression for Displaying Full Name
To display a space between FirstName and LastName, put the space inside double quotes.
NOTE Currently, there is no suggestion when typing into the expression field.
If the content in the expression is not correct, an error will appear with helpful details to fix it.
Figure 29. Incorrect Expression
Common Settings
ID
The internal ID for the column is displayed here only in case it might be required for testing
purposes; otherwise, it holds no value for the user.
Pin Direction
Pin direction is used to make a column pinned on the left or right when the user scrolls the
20

-- 20 of 54 --

overview. By pinning a column, its width will always be a fixed value (in pixels).
SME will re-arrange the columns so that the left pinned columns are always at the beginning of the
column list, and the right pinned ones are at the end.
Figure 30. Columns Order with Pin Direction
Width
The column width can be defined by setting a decimal number starting with 0.3. Steps of 0.1 can be
used. The default setting for the column width is 1.0. When using the default plasma styling, 1.0 is
equivalent to 150 pixels. Accordingly, a width of 0.5 is equivalent to 75 pixels and 2.5 is equivalent
to 375 pixels.
For non-pinned columns, the value defines a minimum width. If there is more space available, all
non-pinned columns will grow proportionally according to their width ratio.
For pinned columns, the value defines a fixed width. Pinned columns will not grow if more space is
available, nor shrink if less space is available. Therefore, please define a sensible size for the
pinned column, so that it will not get bigger than the table itself. Do not overuse the pinned column
since it will always be visible and consume space in the table.
For more details, please consult the Table API from Widgets showcase.
Fixed Width
By default, column widths in Overview Engine are responsive, expanding to fill the remaining
space based on their defined values in the model. Therefore, a column’s width may be different
depending on the browser resolution. However, if fixed width is enabled for the column, the
column width will be fixed at width * 150px (width is the value defined in the model).
Fixed width is always used if the column is pinned.
Column Header
Column header can have Icon and Label to display in three modes: icon only, text only, or a
combination of icon and text.
• To display icon only, define the icon, then check the Hide Label checkbox. The column label will
be used as icon title.
• To display text only, do not define the icon, and make sure the Hide Label checkbox is not
checked.
• To display the combination of icon and text, define the icon, label, and do not check the Hide
21

-- 21 of 54 --

Label checkbox.
To specify an icon, fill the icon name or choose from the icon picker in the Icon field.
For Reference Column, the multilingual labels are automatically filled from the Document Model
containing the selected field in the Element Reference.
Figure 31. Icon and Multilingual Labels in Reference Column
Click the edit button in this repeat to open a detached repeat screen which displays the default label
that is set in the Document Model. Overwrite this label with a new value by typing into the value
field on this detached repeat screen.
Figure 32. Editing the Reference Column Labels
For Expression Column, just enter values in the Value fields in the Label section.
Figure 33. Icon and Multilingual Labels in Expression Column
NOTE • For accessibility certification, if only the icon is displayed, a label should be
22

-- 22 of 54 --

defined and then hidden using the Hide Label checkbox, so that screen readers
can read the column name properly.
• The Material Icons website can be used for easier searching. However, if there is
an icon which is new and not found in our picker, please do not use it.
Alignment
For each column, horizontal and vertical alignments can be set. By default, vertical alignments of
header and content cells are Middle and Top, respectively. The horizontal alignment is Right for
columns with Number data type and Left otherwise.
Figure 34. Alignment
NOTE
For the multi-select column, Overview Engine may render a bullet list for the
default Display Mode, which does not look nice when combined with center or right
horizontal alignment. Currently, there is no nice graphical solution nor a
professional necessity. However, please be aware SME does not forbid this option.
Styles
The Styles section allows adding styles for the header and content cells. Note that styles must first
be set in Styles in order to be available to choose here.
Figure 35. Column Styles
Default Sorting
Default Sorting is the list of sortable columns and their respective preferred sorting, which is used
for sorting the table’s content at the first load of the Overview Engine.
To add a column to Default Sorting, first make sure that it is sortable, then click the Add button
below the section Default Sorting. A drop-down will list the sortable columns; select one to add.
23

-- 23 of 54 --

Figure 36. Default Sorting
NOTE
• The Overview Engine only displays the sort icon (the up or down arrow) for the
first item in the Default Sorting.
• If no default sorting is specified, results are sorted by __meta/createdAt in
descending order.
• Relationship-based default sorting is constrained by Data Services configuration:
◦ mgmtp.a12.dataservices.query.relationshipOrder.maxNestingDepth (default: 5)
◦ mgmtp.a12.dataservices.query.relationshipOrder.maxCount (default: 5)
• For relationship-based sorting, Data Services requires explicit null handling and
rejects invalid paths (for example to-many traversals).
Features
Figure 37. Main Overview Engine Features: Multi-Selection, Button, Search, Filter Bar, and Filter Button
Multi-Selection
Enable Multi-Selection
This checkbox enables the Multi-Selection feature and shows additional available settings.
24

-- 24 of 54 --

Figure 38. Enable Multi-Selection
NOTE Multi-Selection must be further configured in Subheader to display in the
application.
NOTE Range selection via Shift+Click is not supported when Infinite Scrolling is enabled.
In that case, only the individually clicked rows are selected.
Display Multi-Selection
This drop-down defines the display of the Multi-Selection panel, with three options:
• Collapsible (Collapsed): The panel is collapsible and collapsed initially. This is the default
value.
• Collapsible (Expanded): The panel is collapsible and expanded initially.
• Non-Collapsible: The panel is not collapsible. Multi-Selection checkboxes are always visible.
Figure 39. Display Multi-Selection
25

-- 25 of 54 --

Display Counter
This drop-down defines the display of the selected document counter, with two options:
• Simple: The Multi-Selection panel contains the counter when expanded. This is the default
value.
• None: The counter is always hidden.
Figure 40. Display Counter
Interactive Area
This drop-down defines the Interactive Area of the Multi-Selection in the UI, with two options:
• Checkbox and Row: The checkbox and the entire row are clickable to select or deselect the
document.
• Checkbox only: Only the checkbox is clickable to select or deselect the document.
Figure 41. Interactive Area
Actions for Multi-Selection
This section defines the actions that can be applied to selected documents. These actions are shown
like Subheader/Footer buttons but are contained in the Multi-Selection panel.
Clicking the ADD button opens a detached repeat screen to configure the action’s details.
Figure 42. Action for Multi-Selection
NOTE
The configuration of Actions for Multi-Selection is similar to the Subheader or
Footer button’s configuration. An event is linked to the button to perform bulk
operations. The delete_selected event, which removes all multi-selected rows, is
26

-- 26 of 54 --

selectable from the dropdown.
Confirmation Text
Closing the Multi-Selection panel will clear selected rows. To warn the user, use the Display
confirmation before clearing selected rows checkbox. This checkbox is checked by default.
The dialog uses the multilingual titles and messages below. If both fields are empty, the default
engine value is used.
Figure 43. Confirmation Text of Multi-Selection
Search and Filters
Show Full Text Search
Checking this option will make Overview Engine display the text-only search bar. It is checked by
default.
Figure 44. Show Full Text Search
NOTE
When entering "and the" into the Full Text Search, the Overview Engine performs
the following request:
"constraint":{"operator":"simple_search","value":"and the"}
on any indexed field.
For more details about default search behavior, please refer to Simple Search.
27

-- 27 of 54 --

Enable Filter
If this option is set, the field filtering feature will be enabled for the Overview Engine. In
consequence, the Filter Configuration section will be shown in the Overview Model Editor for
further configurations.
Figure 45. Show Filter
NOTE If the Searching and/or Filtering feature is checked, a respective Action Type needs
to be placed in Subheader in order to be displayed in the application.
Filter Configuration
Figure 46. Filter Configuration
Show Filter Button
This option is enabled by default. When this checkbox is checked, the filter button will be displayed
in the desktop mode.
NOTE
• The filter button will always be displayed in the mobile mode.
• If the option is unchecked, Preset Filters should be set by developers.
Show Filter Bar
This option is enabled by default. If the option is unchecked, the filter bar will not be displayed.
Filter Mode
It defines which fields users can filter. The drop-down offers five options:
• All Columns from Overview Model: use field references of all columns in the Overview
Model.
• All Fields from Document Model: use all fields in the content of the Document Model.
• All Fields from Document Model + Meta Data: use all fields in the Document Model,
including metadata fields.
28

-- 28 of 54 --

• Custom Selection of Fields: display an inline repeat to add, remove, and reorder fields
manually. Each repeat item contains the following properties:
◦ Subtype: select a Subtype from the drop-down list. The list contains all subtype models from
the referenced Supertype Model available in the workspace.
◦ Field Reference: select a field from the drop-down list. The list contains fields from the
Document Model. If you select a Subtype, the list contains only fields that are not included
in the referenced Supertype Model.
• Custom Filter: open the Custom Filter Configuration section. Use this mode to model filter
groups, field-based filters, filter definitions, filter selector behavior, filter bar behavior, and the
filter trigger in detail. For project work, this is the recommended filter mode because it provides
the most flexible and future-proof configuration.
Figure 47. Example of Filter Configuration with Subtype
NOTE
• Fields whose data should be available in the filter must be indexed.
• Field filtering supports nearly all repeatable and non-repeatable fields.
◦ Attachments and multi-select based-on string fields are not supported.
• The field label which the filter uses should be the label which Overview Model
configures. If not, the filter will use the label in the Document Model. If a field
has no label and no column label, the Overview Engine will show an empty
string in the filter selector.
• Fields from subtypes of a referenced Supertype Document Model are only
29

-- 29 of 54 --

supported when Filter Mode is set to Custom Selection of Fields or Custom
Filter. In these modes, the available Subtypes in the workspace will be offered
and the respective fields must be specified.
Section Data
This subsection applies to the first four Filter Mode options. When you use Custom Filter, use
Filter Groups instead.
Figure 48. Section in Filter Selector
Sections are used to group selected filters in the Filter Selector .
A section requires a multilingual label and a list of fields defined in Filter Mode.
30

-- 30 of 54 --

Figure 49. Section Data
NOTE
• Adding one field into two different sections is not allowed.
• If a field is not added into any section, it will be shown in the section Other.
Filter String Fields
This subsection applies to the first four Filter Mode options. When you use Custom Filter,
configure the filter item type and options directly in Filter Items.
By default, Overview Engine displays a string field in Filter Selector by a simple text box.
31

-- 31 of 54 --

Figure 50. Normal string field in Filter Selector
Enabling Filter String Fields with Multi-Select will make the configured string field display as
multi-select when filtering.
32

-- 32 of 54 --

Figure 51. Display string field as multi-select in Overview Engine
Filter Normal String Fields
There are two behaviors to perform filtering on string fields: Exact match search and Simple
search. The default behavior is exact match search.
• Exact match looks for the exact value provided for the current string field. Substring or partial
matching is not available. If the field has an annotation with name
enable_case_insensitive_search and value 'true', then a case-insensitive matching is
performed. If not, the case is taken into account. This is the default behavior.
• Simple search uses a case-insensitive substring match algorithm to filter by the current string
field. To activate this mode for a string field, add an annotation with name
enable_approximate_match_search and value 'true' to the field in the respective document
model.
33

-- 33 of 54 --

Figure 52. Annotation enable_approximate_match_search in Document Model
NOTE
There is a difference between Search and Filter and how the Overview Engine
makes use of the Simple search operator.
A Simple search in the Filter only applies to the respective field that has been
marked with annotation enable_approximate_match_search in the Document
Model.
The Overview Engine will split the search term for filtering on whitespace and
composes the following request, for example, with term "and the" for a Field called
"Title":
"constraint":{"operator":"and","value":
[{"operator":"simple_search","fields":["/Title"],"value":"and"},
{"operator":"simple_search","fields":["/Title"],"value":"the"}]}
The Simple search performed from the Search bar takes all indexed fields into
account; regardless of their field type and whether the annotations
enable_approximate_match_search or enable_case_insensitive_search are present or
not. The Display Texts of enumerations are taken into account according to the
locale that the user has chosen in the application. The search term is not split at
white spaces, so the full search term must be preset as a case-insensitive substring
in one of the indexed fields or its enumeration Display Text.
Filter String Fields with Multi-Select
When this feature is enabled, the Overview Engine displays a list of options instead of a simple text
input. This list is paginated by Paging Size. There is a Load more link that appears at the end of the
list to load the next page. If Paging Size is large enough, all options will be loaded, and the Load
more link will be hidden.
Figure 53. Filter String Fields with Multi-Select
34

-- 34 of 54 --

The behavior for filtering string fields with multi-select is based on exact match search with case-
sensitive true.
Custom Filter Configuration
When you select Custom Filter, the Overview Model Editor replaces the classic field list with the
Custom Filter Configuration section. This section gives you full control over the filter structure,
behavior as well as the appearance and end user accessibility of options.
Figure 54. Custom Filter Configuration Overview
Filter Groups
Use Filter Groups to define the sections that users see in the Filter Selector. The Add button creates
a single empty group. The arrow next to Add exposes three generation options:
• Generate from overview columns — creates one group per column defined in the current
Overview Model.
• Generate from document fields — creates one group per field in the referenced Document
Model.
• Generate from document fields + metadata — same as above, but also includes metadata
fields.
35

-- 35 of 54 --

Figure 55. Generate Filter Groups
NOTE The three generate options are only available when a Document Model is
referenced in the Overview Model.
Each filter group contains these main properties:
• ID is generated automatically.
• Name is a technical name for the editor.
• Label is the multilingual title shown in the application.
• Icon adds an icon to the group header.
• Collapsed defines whether the group is collapsed initially.
• Filter Items contains the filters inside the group.
Filter Items
Each group can contain one or more filter items. You can add, copy, remove, and reorder filter
items.
A filter item can be modeled in one of two ways:
• Field Reference binds the item to a field in the referenced Document Model or in a selected
subtype. The filter Type is derived automatically from the field data type.
• Filter Definition creates a query-based filter item instead of a field-based filter. In this case, the
item uses the dedicated Filter Definition editor instead of a field reference.
The following options are available for all filter items:
• ID is generated automatically.
• Type is read-only for field-based filter items and depends on the referenced field. For query-
based filter items, the type is query.
• Show in Filter Bar shows the filter in the Filter Bar.
• Collapsed defines whether the filter is collapsed initially.
• Label and Icon define how the filter is displayed.
Field-based Filter Items
For field-based filter items, the editor shows additional configuration options depending on the
derived filter type. This is important because the available settings are not the same for all data
36

-- 36 of 54 --

types.
The editor only offers the option groups that are valid for the selected field type. Typical examples
are described below.
• Boolean and Confirm filters provide criteria-based configuration. These filter types are used
for simple yes/no style filtering.
Figure 56. Boolean Filter Item Configuration
• String filters provide text-filter settings. One important option is the View Mode. With a text-
input oriented mode, users enter the filter value directly and you can configure matching
behavior such as Exact Match and Case Sensitive. With list view mode, the Overview Engine
loads available string values and presents them as selectable options. This is intended for cases
where the string filter should offer a fixed list of values instead of free-text input.
Figure 57. String Filter Item Configuration with View Mode
• Enumeration and Multi-select filters provide value-oriented configuration such as Initial
Criteria and Pinned Values. For multi-select filters, the editor also offers configuration such as
the join behavior and the item View Mode.
37

-- 37 of 54 --

Figure 58. Enumeration Filter Item Configuration
• Number filters provide configurable Ranges. Use these ranges to define predefined numeric
intervals that users can select instead of entering numbers manually.
Figure 59. Number Filter Item with Ranges
• Date, Date Time, Time, Date Fragment, and Date Range filters provide predefined Ranges.
This is useful when users should choose from common intervals such as exact values, from/to
values, or bounded ranges.
• Date, Date Time, Date Fragment, and Date Range filters also provide predefined Periods. For
example, a date-based filter can offer reusable periods instead of requiring manual date entry
every time.
38

-- 38 of 54 --

Figure 60. Date Time Filter Item with Ranges and Periods
Query-based Filter Items
Query-based filter items are the special case in Custom Filter modeling. They are not derived from a
field data type. Instead, they use the dedicated Filter Definition editor to write the query that
defines the filter.
The editor uses the same query language and grammar support as Query Model filtering. In the
repeat overview, the configured query is displayed as generated text instead of a field path.
39

-- 39 of 54 --

Figure 61. Query-based Filter Item with Filter Definition Editor
Query-based filter items also provide these properties:
• Description defines the text shown for the filter in the application.
• Enabled defines whether the predefined filter is active by default.
NOTE The filter definition editor uses the same language as Query Model filter definitions.
For more information, refer to the Query Modeling Documentation.
Filter Result and Selector
Use Filter Result to control the behaviour and end user options for Match, Invert, and Filter
Selector for how the panel itself is displayed. You can configure the following properties:
• Display Mode with the options Overlay, Docked, and Modal
• Initial Visibility and the option to Show Set Filters Only as well as its end user availability.
You can add a multilingual Subtitle to the Filter Selector, which is displayed below the title.
40

-- 40 of 54 --

Figure 62. Filter Selector Configuration
Enable a Search Bar within the Filter Selector if a long list of filter items is expected. You can
configure if the search bar is initially visible and whether the end user can enable or disable it.
Figure 63. Search Bar Configuration
NOTE If you select Modal as View Mode, do not select Prefer Filter Bar Item for any
Filter Item and do not set Initial Visibility to Show.
Filter Button
Use Filter Button to configure the button that opens the Filter Selector. You can define an icon and
maintain a multilingual label. If you hide the label, the button is displayed as an icon-only button.
The Overview Engine provides a fallback if nothing is configured.
Figure 64. Filter Button Configuration
NOTE For this Filter Mode you don’t need to place an Action Type in Subheader.
41

-- 41 of 54 --

Paging Behavior
There are two options for Paging Behavior: Pagination or Infinite Scrolling.
• Pagination: This is the default option. Enter a value for Paging Size, then in the Overview
Engine, a drop-down called Page Size indicates the number of rows shown on a page. If the
application’s data entries exceed this limit, navigation buttons are added automatically. The
default value for Paging Size is 50.
Figure 65. Pagination
• Infinite Scrolling: With this option, the table loads more rows as users scroll down. Infinite
scrolling can replace pagination in tables to provide better UI/UX in some cases. This option
requires Row Height and Action Column Width.
Figure 66. Infinite Scrolling
NOTE
• Since Data Services support pagination but not offset/limit, the Overview Engine
calculates the page size and page number when Infinite Scrolling is enabled. See
Overview Engine documentation for Developers for more details.
• Page Size must not exceed the query.pageRequest.pageSizeLimit from Data
Services' Query Configuration; otherwise, an error will be returned from Data
Services. To resolve this, either reduce the configured Page Size for Overview or
increase the Data Services' query.pageRequest.pageSizeLimit.
• The same problem occurs when page number is larger than the
query.pageRequest.pageNumberLimit of Data Services' Query Configuration.
Increase the Data Services configuration to resolve it.
Row Height and Action Column Width
Figure 67. Row Height and Action Column Width
• Row Height: Specify a fixed height for the rows. It should be larger than a button height (32px
in the default theme).
42

-- 42 of 54 --

When content is too long, an ellipsis (…) is displayed to indicate that the content was cut. For multi-
select, a comma is used to separate the list instead of a bullet list. When hovering over the cell, a
tooltip is shown to display the full content.
Figure 68. Ellipsis for Fixed Row Height
• Action Column Width: The maximum width of the action column when all buttons are
displayed.
NOTE
If the Action Column Width is not large enough to display all buttons, they will be
cut off.
• For Infinite Scrolling, explicitly set the Action Column Width to ensure all
buttons are visible.
• For Pagination, specifying the Action Column Width is not required. If it is not
set, the action column automatically adjusts its width to fit all buttons.
Columns Resize
Figure 69. Columns Resize
Check the Enable Columns Resize checkbox to make columns resizable. Note that this disables the
responsive feature of columns, meaning all columns will have fixed width.
The action column is not resizable, since its width always depends on its children.
For more details, refer to Resizable Columns Table in the Widgets showcase.
Number of Entries
To show how many entries are displayed in the overview table, check the Show Number of Entries
checkbox.
Figure 70. Configuration for Number of Entries
A number will appear next to the label.
43

-- 43 of 54 --

Figure 71. Number of Entries in the Overview Table
NOTE If there is no label or it is hidden, both the label and number of entries will not be
shown.
Initial Load
Check Skip Initial Load to prevent the overview from querying Data Services until the user applies
a filter or search. The overview will show an empty table until constraints are provided. This avoids
long loading times and overly broad result sets, especially for overviews with large data sets.
Figure 72. Initial Load
NOTE
When using Skip Initial Load, do not set the withoutData property to true in the
corresponding initialActivity of the Application Model. The Overview Engine
handles deferred loading internally. If you used the Application Model to skip initial
load for the Overview in the 2025.06 release line of A12, you must switch to the new
Overview Model’s Skip Initial Load option.
Styles
The Styles section is collapsed by default. It contains a repeat to add a list of logical style names
defined in this model, which can be used to apply custom CSS. For these styles, there are many
Helper Classes listed in the Widgets Showcase.
First, list all styles of the Overview Engine by adding an entry for each one.
Figure 73. Overview Styles
These styles are then available for applying to the individual controls on the Overview Model.
44

-- 44 of 54 --

Figure 74. Apply Styles
Custom Actions
The Custom Actions tab contains sub-sections which define:
• Row Actions, Row Activation, Title for Interactive Rows, and Context Menu
• Minor and major buttons for the Subheader and Footer
• Position for Multi-Selection, Search and Filters
Row Action Group
Figure 75. Row Action Group
The Row Action Group is a container for buttons in each row of the table. Buttons can be defined in
Row Action or Context Menu.
Row Action
Clicking the ADD button in this section opens a detached repeat screen for entering the details for
each row action button.
45

-- 45 of 54 --

Figure 76. Row Action Settings
46

-- 46 of 54 --

Event
Select or enter the event to be linked to the row action. The default event is delete and this is
available to select.
Confirmation Text
A row action button can have multilingual confirmation, including title and messages, which will
be used for a confirmation dialog that appears after clicking this button. This is especially useful in
the case of a delete button. The Overview Engine will not show the confirmation dialog if both title
and message are missing.
Button Styling
Next, configure the row action button style by using the Priority drop-down and Destructive toggle
switch.
Figure 77. Button Styling
• Priority: defines the style (Secondary or Primary) of the button.
• Destructive: sets color. If Destructive is set, the corresponding button will be red. This is useful
to amplify the action, for example, a red Delete button.
All row action buttons will appear as Secondary and non-destructive by default.
Consult the Plasma documentation and the Widgets Showcase for guidelines on button styling.
47

-- 47 of 54 --

Figure 78. Button Styles in Widgets Showcase
Icon
To specify an icon, enter the icon name in the icon picker (the Icon field), then select an icon
accordingly.
Figure 79. Finding Icon Using IconPicker
NOTE The Material Icons website can be used for easier searching. However, if there is an
icon which is new and not found in our picker, please do not use it.
Label and Description
The Label is the text displayed inside a button, while Description is shown when a button is
hovered. Both are multilingual texts and can be configured separately.
Either a label or an icon is necessary for the button to be displayed properly.
NOTE
For accessibility, the aria-label is generated based on Label and Description. For
example, if the given Label and Description are "label" and "description"
respectively, the button’s aria-label will be "label - description".
48

-- 48 of 54 --

Hide Label
By default, Hide Label is not enabled.
When this option is set, the button will be displayed as icon only.
• If Description is not defined, then Label will be shown when hovering the button.
• The defined Label (which is hidden) will be used as the list item text when users right-click on
the row. If Label is empty, the Overview Engine will use Description as the list item text.
Styles
If a row action button has custom styles, define them by using specified Styles in the Overview tab.
Context Menu
Clicking the ADD button in this section opens a detached repeat screen for entering the details for
each group of context menu.
Define the group name and set multilingual labels for the context menu group.
Then add actions for each group. This is similar to adding Row Actions, but without Priority,
Destructive, and Hide Label.
Figure 80. Context Menu
Row Activation
This section defines what happens when users click a row. By default, no Row Activation is
configured and clicking a row does nothing.
49

-- 49 of 54 --

Figure 81. Row Activation
To customize the behavior, select a Type:
• Event: enter a event that can be handled in the application embedding this model. The Event
field is required.
• Non Interactive: the row is not clickable.
Title for Interactive Rows
When hovering over an interactive row, a title will appear to let users know some information,
such as what happens when clicking the row. This multilingual title is also used for accessibility.
Figure 82. Title for Interactive Rows
NOTE If the row is not interactive, for example, if it is disabled or the Overview Engine is
disabled, its title will be empty.
Screen Reader Column
The Screen Reader Column field selects a column whose value is appended to the accessible name
of interactive row elements when announced by a screen reader.
Figure 83. Screen Reader Column
Without this setting, a table with 10 rows each containing a "Delete" button causes a screen reader
to announce "Delete" ten times with no indication of which document is about to be deleted. When
50

-- 50 of 54 --

a screen reader column is configured, the row’s column value is added to the button’s accessible
name via aria-labelledby, so the announcement becomes, for example, "Delete — John Smith".
This applies to:
• Row action buttons — all buttons in the row action column
• Multi-selection checkboxes — when multi-selection is enabled
NOTE If no screen reader column is selected, no additional context is added to row action
buttons or checkboxes.
NOTE
Avoid selecting boolean or expression columns as the screen reader column.
Boolean columns produce values like "true/false" which are not meaningful when
read aloud. Expression columns may contain HTML markup (e.g. <p> tags) that will
be included verbatim in the announcement.
Subheader
Figure 84. Subheader Section
By clicking the ADD button, a detached repeat screen is opened to create a respective action type:
Button, Search, Filter, or Multi-Selection.
51

-- 51 of 54 --

Figure 85. Choose Type for Subheader Action
52

-- 52 of 54 --

NOTE There is only one action of each Search, Filter, or Multi-Selection allowed across the
Subheader.
Subheader Button
If the action type is Button, additional configurations will be shown.
Define an event that is linked to the button. The add event is selectable by default. Type the event
name into the autocomplete to define a custom event.
Button configuration is similar to Row Action.
Footer
The available elements for Footer are Button only, and its configuration is similar to Row Action.
Refactoring
The Simple Model Editor supports automatic refactoring for Overview Models. When you rename,
move, or delete elements that are referenced elsewhere, the tool identifies affected references and
presents options for updating them.
For detailed information about refactoring capabilities, workflow, and actions, refer to the
Refactoring Support section in the SME Tool Documentation.
Overview Model Specific Refactoring
Overview Model refactoring handles references within Overview Models when columns, filters, or
actions are modified. The following elements and reference types are supported:
Supported Operations
Overview Model refactoring supports the following operations:
• Deleting or renaming columns
• Modifying default sorting configuration
• Deleting filter fields
• Enabling or disabling feature buttons
• Modifying style references
Reference Types
The following reference types are tracked and updated during Overview Model refactoring:
53

-- 53 of 54 --

Reference Type Description
Column Element
References
References from columns to Document Model fields.
Default Sorting References to columns used in default sorting configuration. Columns
removed from the model or marked as non-sortable are removed from
default sorting.
Filter Field References References to fields used in filter configuration.
Style References References from columns to style definitions.
Event References References in row actions, context menu actions, and toolbar buttons.
Query Model
References
References to Query Models used as data sources.
Document Model
References
References to primary and sub-document models.
Example: Deleting a Column
Consider an Overview Model with:
• A column named statusColumn configured as sortable
• Default sorting configured to sort by statusColumn
When you delete statusColumn:
1. Save the Overview Model.
2. The refactoring dialog displays the affected default sorting reference.
3. Choose one of the following actions:
◦ Commit: Remove the column from the default sorting configuration.
◦ Edit: Select a different column for default sorting.
4. The model saves with your selected changes applied.
Example: Disabling a Feature
When you disable features such as multi-selection or search:
1. The refactoring system detects associated buttons and configurations.
2. The refactoring dialog displays the affected elements.
3. Feature-related buttons are removed from the configuration.
For information about enabling or disabling Overview Model refactoring, see Enabling Within-
Model Refactoring in the SME Tool Documentation.
54

-- 54 of 54 --

